import { Box } from "@chakra-ui/core";
import React, { ReactElement } from "react";

export default function CreatePlaylist(
  props: Record<string, any>[]
): ReactElement {
  return <Box w="10%" h="100%"></Box>;
}
